<%= turbo_frame_tag "like_posts-list" do %>

  <div class="container pt-3">
    <div class="row">
      <div class="col-12">
        <div class="row">
          <% if @like_posts.present? %>
            <%= render @like_posts %>
          <% else %>
            <p><%= t('.no_result') %></p>
          <% end %>
        </div>
      </div>
      <div class="d-flex justify-content-center pt-4">
        <%= paginate @like_posts, theme: 'bootstrap-5' %>
      </div>
    </div>
  </div>
<% end %>